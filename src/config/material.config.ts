import type { MaterialConfig } from '../types/book.types'

export const defaultMaterialConfig: MaterialConfig = {
  coverRoughness: 0.6,
  coverMetalness: 0.0,
  coverFinish: 'matte',
  spineRoughness: 0.6,
  spineMetalness: 0.0,
  coverNormalMapFile: null,
  coverRoughnessMapFile: null,
}
